{
  "schema": 1,
  "note": "Accounts (phase 3, September 6, 2026). `firebase` is the web-app config of the PokoPal Firebase project, written by tools/setup_firebase.sh. It is public by design (Firebase's own guidance); what protects the data is firestore.rules at the project root. While it is null the app works signed out and the Sign in row says accounts are not set up. `sdk` is the Firebase JS SDK version loaded from Google's CDN (www.gstatic.com/firebasejs); bump it here, never in code.",
  "sdk": "12.9.0",
  "firebase": null
}
